# 闭环电机速度控制使用说明

## 概述

本项目已成功实现基于编码器反馈的闭环电机速度控制，替换了原有的开环PWM占空比控制模式。新的控制系统使用PID控制器来精确控制电机转速，提供更好的速度跟踪性能和稳定性。

## 实现特点

### 1. 基于现有PID架构
- 使用项目中已有的 `sLib_PosPID_t` 位置式PID控制器
- 参考 `sAPP_BlcCtrl.cpp` 中的速度环PI控制器实现模式
- 保持与现有代码风格和架构的一致性

### 2. 双电机独立控制
- 左右电机各自拥有独立的PID控制器
- 支持差速控制，实现转向功能
- 编码器反馈：`sDRV_GMR_GetLeftRPM()` 和 `sDRV_GMR_GetRightRPM()`

### 3. 兼容性设计
- 保留原有的直接PWM控制方法（`setLM()`, `setRM()`）
- 可以在闭环和开环控制模式之间切换
- 不影响现有代码的使用

## API接口

### 基本控制方法

```cpp
// 启用/禁用闭环控制
motor.enableClosedLoopControl(true);   // 启用闭环控制
motor.enableClosedLoopControl(false);  // 禁用闭环控制（回到开环模式）

// 设置目标转速（RPM）
motor.setLeftTargetRPM(30.0f);          // 设置左电机目标转速
motor.setRightTargetRPM(30.0f);         // 设置右电机目标转速
motor.setTargetRPM(30.0f, 30.0f);       // 同时设置左右电机目标转速

// 获取目标转速
float left_target = motor.getLeftTargetRPM();
float right_target = motor.getRightTargetRPM();
```

### PID参数调整

```cpp
// 设置PID参数
motor.setPIDParams(0.8f, 0.1f, 0.05f);  // 同时设置左右电机PID参数
motor.setLeftPIDParams(0.8f, 0.1f, 0.05f);   // 单独设置左电机PID参数
motor.setRightPIDParams(0.8f, 0.1f, 0.05f);  // 单独设置右电机PID参数
```

### 便捷运动控制方法

```cpp
// 基本运动控制
motor.moveForward(30.0f);    // 前进，30 RPM
motor.moveBackward(30.0f);   // 后退，30 RPM
motor.turnLeft(20.0f);       // 左转，20 RPM
motor.turnRight(20.0f);      // 右转，20 RPM
motor.stop();                // 停止
```

### 控制循环更新

```cpp
// 在控制任务中定期调用（建议20ms周期，50Hz）
const float CONTROL_DT = 0.02f;  // 20ms
motor.updateClosedLoopControl(CONTROL_DT);
```

## 使用示例

### 基本使用流程

```cpp
void motor_control_example()
{
    // 1. 初始化电机
    motor.init();
    
    // 2. 启用闭环控制
    motor.enableClosedLoopControl(true);
    
    // 3. 设置PID参数（可选，有默认值）
    motor.setPIDParams(0.8f, 0.1f, 0.05f);
    
    // 4. 在控制循环中使用
    for (;;) {
        // 更新编码器数据
        motor.update();
        
        // 设置目标转速
        motor.moveForward(30.0f);
        
        // 执行闭环控制更新
        motor.updateClosedLoopControl(0.02f);
        
        // 延时20ms
        vTaskDelay(20 / portTICK_PERIOD_MS);
    }
}
```

### 差速转向示例

```cpp
void differential_steering_example()
{
    motor.enableClosedLoopControl(true);
    
    // 前进
    motor.setTargetRPM(30.0f, 30.0f);
    vTaskDelay(2000 / portTICK_PERIOD_MS);
    
    // 左转（左轮慢，右轮快）
    motor.setTargetRPM(10.0f, 30.0f);
    vTaskDelay(1000 / portTICK_PERIOD_MS);
    
    // 原地左转（左轮反转，右轮正转）
    motor.setTargetRPM(-20.0f, 20.0f);
    vTaskDelay(1000 / portTICK_PERIOD_MS);
    
    // 停止
    motor.stop();
}
```

## 默认参数

### PID参数
- Kp = 0.8（比例增益）
- Ki = 0.1（积分增益）
- Kd = 0.05（微分增益）

### 控制参数
- 控制周期：20ms（50Hz）
- PWM输出范围：-100% 到 +100%
- 积分限幅：-50 到 +50

## 调试和监控

当前实现包含详细的调试输出，可以监控：
- 目标转速 vs 实际转速
- 控制误差
- PID输出值

调试信息通过UART输出，格式如下：
```
[MOTOR] 目标[左:30.0,右:30.0] 实际[左:29.8,右:30.2] 误差[左:0.2,右:-0.2]
```

## 注意事项

1. **控制周期**：建议使用20ms控制周期，确保PID控制器有足够的更新频率
2. **PID调参**：根据实际电机特性调整PID参数，当前参数为初始值
3. **安全保护**：系统保留了原有的电机保护功能（刹车、限幅等）
4. **编码器精度**：确保编码器正常工作，提供准确的转速反馈

## 任务配置

电机控制任务已启用并配置为：
- 任务名：`MotorCtrl`
- 堆栈大小：4096字节
- 优先级：4（高优先级）
- 控制频率：50Hz

该任务会自动运行测试序列，循环测试不同转速的控制效果。
