# UART6主机通信驱动测试说明

## 概述

UART6主机通信驱动已经集成到应用层任务系统中，可以直接进行测试。该驱动使用文本协议，易于阅读和调试。

## 测试准备

### 1. 编译和下载
- 编译项目并下载到STM32F405开发板
- 确保UART6硬件连接正确

### 2. 串口工具设置
- **调试串口（UART1）**：用于查看测试输出
  - 波特率：115200
  - 数据位：8
  - 停止位：1
  - 校验位：无

- **通信串口（UART6）**：用于发送测试数据
  - 波特率：115200
  - 数据位：8
  - 停止位：1
  - 校验位：无

## 测试步骤

### 1. 启动系统
系统启动后，UART6测试任务会自动运行，你会在调试串口看到：
```
=== UART6主机通信测试任务启动 ===
[INFO]UART6驱动初始化完成，开始接收数据
[INFO]请通过UART6发送以下测试数据包:
  轮速数据: {cmd:wheel,lw:1.5,rw:2.0}
  步进电机: {cmd:stepper,s1:1000,s2:-500}
  组合数据: {cmd:combined,lw:1.2,rw:1.8,s1:800,s2:200}
  心跳包: {cmd:heartbeat}
```

### 2. 发送测试数据包
通过UART6串口工具发送以下测试数据包：

#### 轮速控制数据包
```
{cmd:wheel,lw:1.5,rw:2.0}
```
**预期输出**：
```
[DEBUG]UART6_HostComm: 收到数据 (25字节): {cmd:wheel,lw:1.5,rw:2.0}
[INFO]UART6_HostComm: 轮速数据 - 左:1.50, 右:2.00
[UART6]轮速数据: 左=1.50, 右=2.00, 时间戳=12345
```

#### 步进电机位置数据包
```
{cmd:stepper,s1:1000,s2:-500}
```
**预期输出**：
```
[DEBUG]UART6_HostComm: 收到数据 (28字节): {cmd:stepper,s1:1000,s2:-500}
[INFO]UART6_HostComm: 步进电机位置 - 电机1:1000, 电机2:-500
[UART6]步进电机位置: 电机1=1000, 电机2=-500, 时间戳=12346
```

#### 组合数据包
```
{cmd:combined,lw:1.2,rw:1.8,s1:800,s2:200}
```
**预期输出**：
```
[DEBUG]UART6_HostComm: 收到数据 (38字节): {cmd:combined,lw:1.2,rw:1.8,s1:800,s2:200}
[INFO]UART6_HostComm: 组合数据 - 轮速[左:1.20,右:1.80] 步进电机[1:800,2:200]
[UART6]组合数据: 轮速[左:1.20,右:1.80] 步进电机[1:800,2:200]
```

#### 心跳包
```
{cmd:heartbeat}
```
**预期输出**：
```
[DEBUG]UART6_HostComm: 收到数据 (15字节): {cmd:heartbeat}
[INFO]UART6_HostComm: 收到心跳包
```

### 3. 观察定期输出
系统会定期输出统计信息和状态：

#### 每10秒的统计信息
```
[UART6]统计信息: 总包=4, 有效=4, 错误=0, 超时=0
[UART6]数据有效性: 有效
```

#### 每30秒的心跳包发送
```
[UART6]发送心跳包
[INFO]UART6_HostComm: 发送响应: {cmd:heartbeat,ts:12345}
```

## 测试验证点

### 1. 数据包解析正确性
- ✅ 能正确解析轮速数据包
- ✅ 能正确解析步进电机数据包
- ✅ 能正确解析组合数据包
- ✅ 能正确处理心跳包

### 2. 回调函数执行
- ✅ 轮速数据回调函数被正确调用
- ✅ 步进电机数据回调函数被正确调用
- ✅ 组合数据回调函数被正确调用
- ✅ 错误回调函数在出错时被调用

### 3. 数据存储和获取
- ✅ 数据被正确存储在驱动程序中
- ✅ 可以通过API获取最新数据
- ✅ 数据有效性检查正常工作

### 4. 统计信息
- ✅ 总包数统计正确
- ✅ 有效包数统计正确
- ✅ 错误包数统计正确
- ✅ 超时错误统计正确

### 5. 发送功能
- ✅ 心跳包发送正常
- ✅ 状态响应发送正常

## 错误测试

### 1. 发送格式错误的数据包
```
{cmd:wheel,lw:1.5}  # 缺少rw字段
```
**预期输出**：
```
[ERROR]UART6_HostComm: Missing key
[UART6]错误: 代码=3, 消息=Missing key
```

### 2. 发送无效命令
```
{cmd:invalid,data:123}
```
**预期输出**：
```
[ERROR]UART6_HostComm: Invalid command
[UART6]错误: 代码=2, 消息=Invalid command
```

### 3. 发送格式错误的数据包
```
{cmd:wheel,lw:1.5,rw:2.0  # 缺少结束括号
```
**预期输出**：
```
[ERROR]UART6_HostComm: Invalid format
[UART6]错误: 代码=1, 消息=Invalid format
```

## 故障排除

### 1. 没有输出信息
- 检查调试串口连接和波特率设置
- 确认任务已正确创建和启动

### 2. 收不到数据
- 检查UART6硬件连接
- 确认UART6波特率设置正确
- 检查数据包格式是否正确

### 3. 解析失败
- 确认数据包格式符合协议规范
- 检查是否有多余的空格或字符
- 确认使用的是英文标点符号

### 4. 回调不执行
- 检查回调函数是否正确设置
- 确认数据包解析成功
- 查看错误输出信息

## 扩展测试

### 1. 压力测试
连续快速发送多个数据包，测试系统的处理能力：
```
{cmd:wheel,lw:1.0,rw:1.0}
{cmd:wheel,lw:1.1,rw:1.1}
{cmd:wheel,lw:1.2,rw:1.2}
...
```

### 2. 边界值测试
测试极值数据：
```
{cmd:wheel,lw:-999.99,rw:999.99}
{cmd:stepper,s1:-2147483648,s2:2147483647}
```

### 3. 长时间运行测试
让系统运行数小时，观察：
- 内存是否有泄漏
- 统计信息是否正确
- 系统是否稳定

## 成功标准

测试成功的标准：
1. 所有类型的数据包都能正确解析
2. 回调函数都能正确执行
3. 统计信息准确无误
4. 错误处理机制正常工作
5. 系统长时间运行稳定
6. 发送功能正常工作

完成以上测试后，UART6主机通信驱动程序就可以投入实际使用了！
